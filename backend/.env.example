PORT=5000
NODE_ENV=development
JWT_SECRET=fundsroom-super-secret-key-2026
# Optional: Set to use a local or remote PostgreSQL instance. If omitted, embedded WASM PostgreSQL is used automatically.
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fundsroom_erp
